```
#启动nginx
cd "F:/software/nginx-1.20.2" && start nginx
# 停止Nginx
cd "F:/software/nginx-1.20.2" && nginx -s stop
# 重新加载配置（修改配置后执行）                                                 cd "F:/software/nginx-1.20.2" && nginx -s reload
# 验证配置语法
cd "F:/software/nginx-1.20.2" && nginx -t
```

## 启动
```
nginx
```